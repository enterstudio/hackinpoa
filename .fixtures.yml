fixtures:
    forge_modules:
        stdlib: "puppetlabs/stdlib"
        api: "tsuru/api"
        router: "tsuru/router"
        docker: "tsuru/docker"
        apt:
            repo: "puppetlabs/apt"
            ref: 1.6.0
        base:
            repo: "tsuru/base"
            ref: 0.0.4
        firewall:
            repo: "puppetlabs/firewall"
            ref: 1.1.3
    symlinks:
        tsuruio: "#{source_dir}/modules/tsuruio"
